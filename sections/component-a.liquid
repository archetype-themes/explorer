{% render 'component-a', title: section.settings.title %}

{% schema %}
{
  "name": "Component A",
  "class": "index-section",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "t:labels.announcement_text",
      "default": "Component A"
    }
  ],
  "presets": [
    {
      "name": "Component A"
    }
  ]
}
{% endschema %}
